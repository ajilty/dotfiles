# Credentials for agent harnesses and the MCP servers they spawn.
#
# Interactive shell:  env agents
# Wrap a command:     with-secrets agents -- opencode
#                     with-secrets agents -- codex
#
# Values are 1Password references, resolved at runtime; no secret lands here.

export CONTEXT7_API_KEY="op://gits/Context7/API Keys/1pass_synced"
